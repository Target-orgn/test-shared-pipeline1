name: rani-ukamble-group/test_file
on:
  workflow_call:
jobs:
  test_file:
    runs-on: ubuntu-latest
    container:
      image: alpine:latest
    timeout-minutes: 60
    steps:
    - uses: actions/checkout@v4.1.0
      with:
        fetch-depth: 20
    - run: echo "Running tests..running"
